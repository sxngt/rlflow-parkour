# RLflow 기본 config = 현재 기준선 (P3-70 control, shared-course). 다른 실험은 --config-name <json 이름> 으로.
defaults:
  - p3-70-control
  - _self_

# 평가 suite 기본값 (evaluate-promote 는 config_name 태그가 없으면 config.yaml 을 읽는다)
eval:
  default_suite: eval_suite/curriculum.yaml
