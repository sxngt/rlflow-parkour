# P3-16: reach curriculum and separated whole-platform gap

Predeclared capability extension after P3-15: same-seed checkpoint800 initializes policy/critic/normalization, with fresh optimizer/RNG/curriculum. Two seeds,1200updates each,1024environments,24rollout steps. Range grows0–30cm atupdate0,0–40cm at300,0–50cm at600; update boundaries reset incomplete episodes under existing curriculum contract. Exploration cap0.1 then0.05 at900. Robot, action limits, rewards,3cm launch region, first-contact precision, stability and minimum airborne displacement criterion remain unchanged. Budget58,982,400 new environment steps. Native fixed reach evaluation15/25/35/45/50cm; common continuous regression follows. These are development experiments, not a final locked test or champion promotion.

Full-platform gap is an opt-in evaluation adapter, not yet a training terrain. Two disjoint1m-long platforms contain whole initial and translated four-foot stances with2cm edge margin. Calibrated footprint span0.3735075m means50cm translation gives0.0864925m physical gap. Global catch floor is50cm lower. USD collision bounds and downward PhysX ray queries on both platforms and in the empty gap must agree for first/last environment. Successful crossing additionally requires last preflight upward support on departure for every foot and first/final contacts on the selected landing platform. The original airborne body displacement and strict flight/precision/stability gates also remain required. Last support positions are foot-center proxies, not measured contact patches. No whole-gap success is assumed before execution.

Run a64-environment smoke before main training; smoke iteration12 does not exercise later curriculum transitions, which require the existing schedule tests and main-run records. Evaluate a frozen P3-15 policy on the new geometry as a negative capability probe while training is prepared. Preserve failed attempts. All videos64env/camera4, detailed result titles. Current high-speed, approach-jump, slope, turn and physical rollout planner goals remain incomplete.
