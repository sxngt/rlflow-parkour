# P4-03: history information under identical input/network dimensions

Use the P4-02 low-friction three-hop training terrain (station2/3 authored0.2) and the same-seed P3-08 mapped parent checkpoints. Compare eight-frame observation stack versus zero-history control, seeds1/2. Both actors/critics have535 inputs: current66 +previous7×66 +7 validity bits; identical128×128 hidden layers/PPO/reward/budget. Extra actor/critic first-layer columns start at zero; original66 columns and all other policy/critic weights are copied. This preserves initial mean-policy behavior within numerical tolerance. Shared66-channel normalization applies current-frame moments to all valid frames, updates moments only from the current frame per call, and zeros invalid history after normalization. Original normalization state is retained.

History covers current plus previous seven50Hz samples (140ms). Duplicate observation reads at the same episode clock do not advance history; skipped/backward clocks clear it. Episode reset clears only affected rows; intermediate successful hop transitions preserve history. Validity masks distinguish missing initial frames. No future samples, terrain friction labels or hindsight failure data enter observations. Resume restores model/optimizer/normalization/RNG at a new episode boundary, so history starts empty as documented.

The zero control has the same declared parameter count and matrix sizes; inactive input columns receive no informative gradient, so identical useful representational capacity is not claimed. Both conditions retain the existing oracle-state/contact-derived phase channels. This isolates added short history in this simulation contract, not a clean no-contact B0 or deployable sensor-only policy.

Main budget:1024env×24×800×4=78,643,200 steps. Require clock/mask/normalization/initial-mean tests,64-env12-update smoke,2-update resume and native evaluation before main training. Compare final models on the same16 P4-02 suites (uniform8,0.2,0.05,continuous regression), plus clearly separate native results. No inference of general history benefit from one seed or reward. Record parameter count, throughput, GPU memory, model/normalizer contracts and titled64-robot videos. Keep the same parent and physical conditions in both arms; do not initialize from the better-looking P4-02 candidate.
