# P3-36 — Explore inside effective action limits

P3-34 full45cm-gap update600 mean evaluation:0/16full,3transfers then failure,43.75%input action components exceed the environment clamp, peak absolute action3.825 while limit1. This is input-command clipping, not measured actuator torque saturation. With small Normal noise around an out-of-range mean, many sampled commands map to the same actual joint reference. It is a suspected exploration limitation, not proven sole cause.

New optional bounded_mean_gaussian_v2 uses Normal(tanh(actor_output), bounded_std) consistently in rollout, PPO likelihood and mean evaluation. It is not a tanh-transformed action distribution: Normal samples can still exceed limits and the environment still clamps them. The bounded mean allows inward exploration even for large actor logits. Legacy v1 behavior remains unchanged. No robot or motor limits are widened. Tests verify inference/rollout mean identity, unchanged same-policy likelihood, finite nonzero actor gradient and within-limit samples from a saturated actor; legacy means remain identical.

Paired engineering comparison: same75%-gap parent checkpoint800, same seed, full-gap geometry, optimizer initialization LR, budgets and std schedules. Only mean contract differs. Both copy identical weights and normalization; v2 explicitly does not preserve the initial actor function. This is one learning seed, not a replicated research conclusion. Smoke12×64 first; then800×1024 per condition if integration passes. Clip fraction is recorded in training and first-episode evaluation.
