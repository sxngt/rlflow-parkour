# P3-19 long scenarios and third-person recording

User steering: roughly10 dynamic terrain moves/jumps, runs longer than10seconds, difficulty tiers, and single-robot third-person follow recording instead of parallel views. Supersedes the previous parallel64/camera4 preference for new final evaluations. Physics training remains batched. The prior P3-18 short five-surface main pilot was prepared but NOT launched; do not launch it now.

Three deterministic development map templates now use11 shared surfaces /10 inter-surface transfers. Seed1 approximate path lengths: easy4.64m,medium6.52m,hard8.59m. Nominal center spacing45/65/85cm, roll/pitch limits3/10/20degrees and heading variations6/15/25degrees per step; height variation scales4/12/24cm. These are geometry bounds, not achieved performance. Easy blocks may overlap in ground projection; medium/hard increase separation. Selected points must be ray-audited against intended physical surfaces before evaluation/training. Geometry feasibility beyond local point checks is not assumed. Maps contain height, slope and turning from the beginning; complexity will be expanded based on actual policy results.

Episodes allow30s. Ten surface transfers are logged separately from measured jumps; the physics-rate jump detector requires prior support,>=20ms no-foot flight,>=3cm upward excursion, upward speed>0.2m/s, subsequent foot support and no observed non-foot force>5N during flight. These are measured flight/landing events, not inferred from target index. Successful long demonstration eligibility requires full course completion,10transfers andactual elapsed>=10s. A failed/short episode is preserved and labeled as such; it is not padded with resets to meet duration. If a future successful policy finishes faster, extend the scenario rather than artificially force waiting.

New configs default evaluation_camera_mode=follow. One fixed evaluation robot is render-enabled; other evaluated robots remain hidden. Camera follows filtered body yaw/position from2.5m behind,0.85m to the side,1.4m above the body, looking0.5m ahead. MP4 is original frames at25fps; each frame records simulation timestamp,root pose andcamera pose. End recording at the followed first episode's termination. Other evaluation episodes continue for complete statistics. Captions/result titles explicitly identify third-person follow. No success-based video selection is implied.

Training plan: first validate new long-map+camera learning/resume/evaluation path, then fresh PPO easy-map seeds1/2,3000updates each,1024env×24rollout,147,456,000newsteps. This budget is a first pilot, not a promise of learned ten-jump runs. Native30s fixed-map evaluations generate follow MP4. Follow-up evaluations change terrain seeds/difficulty through a recorded runtime adapter, preserving checkpoint training config. Increase difficulty after demonstrated progress, retaining easier courses. Do not claim a scripted contact sequence is an autonomous Planner.

Initial long easy smoke failed the physical target-ray audit: the previous block occluded a terminal rear-foot target. This attempt is preserved. The final center transfer now has a60cm minimum to clear the previous block; independent local ray/slab checks also reject hidden contact scripts before simulator startup. Geometry is still not a global feasibility proof.
