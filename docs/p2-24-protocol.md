# P2-24 학습 후반 행동 잡음 상한 축소

P2-23에서 P2-20 seed2/3의 같은 고정 모델·scenario가 평균 행동보다 샘플링 행동에서 더 많이 성공했다. 평균 행동의 착지 유지가 학습 때의 잡음에 의존하는지 추가 검증한다. 샘플링 평가가 더 좋았다는 관찰만으로 잡음을 줄인 학습이 개선된다고 단정하지 않는다.

P2-20 대비 마지막400update(1201–1600)의 std상한만0.10→0.05로 변경한다. 하한0.05를 유지하므로 이 구간의 실제std는0.05로 고정된다. 초기1–800 상한0.35,801–1200 상한0.20, 평균 발 정밀도 보상과 결합 최초접촉 보상, 거리/반경 커리큘럼·환경·평가·PPO는 유지한다. P2-22의 최악 발 보상은 도입하지 않는다.

fresh4seed×1024환경×24step×1600update, 각39,321,600step/추가157,286,400step. P2-20 네seed를 동일예산 대조로 재사용한다. 탐색 감소에 따른 도약·목표 유지의 개선과 기존 능력의 회귀를 모두 보고한다. 최종mean 평가0/5/10/15cm 각16개를 유지한다. 추가로최종모델의sampled RNG20000/20001을 같은64개개발군에서 평가해mean/sample차이를확인한다. 대조의sampled결과는P2-23에서재사용한다. RNG반복은독립학습seed가아니다.

실제PPO 분포의 floor=cap 경우 sampling/logprob/entropy/update의 일관성과 buffer복원을 검사한다. 짧은3update의축소일정에서0.35→0.2→0.05를확인하고최종평가/영상파이프라인을검증한다. 주요run의1201전환을별도로확인한다. 기존엄격exploration checkpoint계약을유지한다.

각최종평가에200Hz진단·64개렌더링/camera_side4·상세제목result영상·phase:P2/step:p2-24-late-noise-reduction을남긴다. 성능을확인하기위해예산연장/seed교체/평가조건완화를하지않는다. 목표를완수하지못하면기각하고실패사례를보존한다.
